#js in console-
1. used with alert to display a message- alert('hi')
2. can be used as calculator
#dom manipulation-document objeect model(change html,css, perform actions)
html-document.body.innerHTML='<b>welcome</b>'
css-document.getElementsByTagName('b')[0].style.fontSize= '50px'
#CHROME EXTENSIONS-1. create features
2. interact with web
3.api access
4. user experience
eole of browser->turns html code into what u see on screen
>helps interact with webpage, allows changes through js
>gets html, images, etc from the server
js alters page without reloading
fetches and sends info to a server
it runs at client side in the browser
coffescript/typescript are transpiled into js
strings in js are defined using single  quotes, double quotes or backtick
backtick has an advantage that it can be used when one needs to manipulate string
string and number can be concatenated using +
'you have got'+1+1+1+'votes'//you have got 111 votes
'you have got'+(1+1+1)+'votes'// you have got 3 votes
typeof-retunrs number, string, boolean, etc
eg- typeof 3//number
answers of practice exercise-
1. ` Your final bill is ₹ ${(1*100)+(2*10)+(1*50)}`
2. `₹ ${(79990+2*24990)*18/100}`
3 'method 1', "method 2", `method 3`

# Javascript with html & css
1. document.getElementbyId('name')
2. document.getElementbyClassName('')
3. document.querySelector('h1')
4. document.querySelectorAll('.classname')-returns list